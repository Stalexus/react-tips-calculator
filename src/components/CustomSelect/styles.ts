export const selectStyles = {
    control: (styles: any) => ({
        ...styles,
        marginTop: '20px',
        textAlign: 'center',
        color: '#756C6C',
        borderRadius: '30px',
        paddingTop: '15px',
        paddingBottom: '15px',
    }),
    indicatorSeparator: (styles: any) => ({
        ...styles,
        display: 'none',
    })
}